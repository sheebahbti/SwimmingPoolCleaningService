{
  "name": "swimming-pool-cleaning-service",
  "version": "1.0.0",
  "private": true,
  "description": "Swimming Pool Cleaning Service - Full Stack Application",
  "scripts": {
    "install:all": "cd frontend && npm ci --include=dev && cd ../backend && npm ci --include=dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "cd backend && npx prisma migrate deploy && node dist/index.js",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "postinstall": "npm run install:all"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
